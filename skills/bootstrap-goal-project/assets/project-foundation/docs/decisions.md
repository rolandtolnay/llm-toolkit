# Implementation decisions

Lightweight, non-obvious implementation choices future runs should not repeatedly reopen. Use `docs/adr/` when a decision is hard to reverse, surprising without context, and based on a real tradeoff. Product choices belong in `PROJECT.md`.

## <YYYY-MM-DD> — <decision title>

**Decision:** <choice>.

**Why:** <reason and runner-up if useful>.

**Revisit when:** <specific condition, or omit when not meaningful>.
